# Pinned versions for reproducible builds.
# To upgrade: bump here, install, run tests, then commit the new pins.
# Generate a fully-resolved lockfile with: pip freeze > requirements.lock

# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
slowapi==0.1.9

# Supabase
supabase==2.10.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv==1.0.1

# Validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# File handling
aiofiles==24.1.0
pillow==11.1.0
# Excel export — Crew Monthly Flight Hours Matrix (professional 5-sheet workbook).
openpyxl==3.1.5

# Utilities
python-dateutil==2.9.0
pytz==2024.2
babel==2.16.0

# Outbound HTTP — supabase 2.10 pins httpx<0.28, so we don't add our own
# pin; the supabase transitive dep covers the import. If you upgrade
# supabase past 2.13, re-evaluate whether to pin httpx explicitly.

# Push Notifications (FCM) — optional at runtime.
# When FIREBASE_SERVICE_ACCOUNT_JSON env var is unset, push_service falls
# back to STUB mode and the import is never reached (lazy).
firebase-admin==6.6.0

# Observability dashboard (Phase 1).
# JSON-formatted logs so the dashboard + downstream log shippers can parse
# them without regex; APScheduler runs the metrics rollup + retention pruner
# inside the API process; psutil reads host CPU/RAM/disk for /admin/health.
python-json-logger==2.0.7
APScheduler==3.10.4
psutil==6.1.1
# 2FA TOTP — RFC 6238 codes (Google Authenticator / Authy / 1Password).
pyotp==2.9.0
