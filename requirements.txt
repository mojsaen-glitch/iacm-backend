# Pinned versions for reproducible builds.
# To upgrade: bump here, install, run tests, then commit the new pins.
# Generate a fully-resolved lockfile with: pip freeze > requirements.lock

# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
slowapi==0.1.9

# Supabase
supabase==2.10.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv==1.0.1

# Validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# File handling
aiofiles==24.1.0
pillow==11.1.0

# Utilities
python-dateutil==2.9.0
pytz==2024.2
babel==2.16.0

# Outbound HTTP (Sprint 6 — Open-Meteo weather lookups). FastAPI already
# pulls httpx transitively but pin it so the version doesn't drift.
httpx==0.28.1

# Push Notifications (FCM) — optional at runtime.
# When FIREBASE_SERVICE_ACCOUNT_JSON env var is unset, push_service falls
# back to STUB mode and the import is never reached (lazy).
firebase-admin==6.6.0
